using com.ArkAngelApps.TheAvarice.Scriptable.Characters;
using UnityEngine;

namespace com.ArkAngelApps.TheAvarice.Handlers.Character
{
	public sealed class PlayerAnimation : CharacterAnimation
	{
			/*
		private void FixedUpdate()
		{
			if (PlayerRuntimeData.Instance.character.movement.moveAxis == Vector2.zero)
			{
				Play(GetDefaultIdle());
			} else if (PlayerRuntimeData.Instance.character.movement.moveAxis == Vector2.down)
			{
				Play(anims.walkDown);
			} else if (PlayerRuntimeData.Instance.character.movement.moveAxis == Vector2.up)
			{
				Play(anims.walkUp);
			} else if (PlayerRuntimeData.Instance.character.movement.moveAxis == Vector2.left)
			{
				Play(anims.walkLeft);
			} else if (PlayerRuntimeData.Instance.character.movement.moveAxis == Vector2.right)
			{
				Play(anims.walkRight);
			}
		}
			*/
	}
}